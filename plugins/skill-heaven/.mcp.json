{
  "mcpServers": {
    "skill-summon": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/skill-summon.mjs"],
      "env": {
        "SKILL_SOURCE": "${user_config.skill_url}"
      }
    }
  }
}
