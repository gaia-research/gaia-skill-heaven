# Lucy v4 owner approval

Date: 2026-08-14
Scope: all nine active Heaven / Hell / Ultra v4 visual variations in
`packages/site/src/assets/lucy/v4-candidates/CANDIDATE_MANIFEST.json`

## Verdict

The owner explicitly approved every active variation and authorized downstream
production:

> Verdict: all pass :) I actually want all variations. Proceed with downstream
> using terra and luna agents only.

This verdict promotes the nine active visual candidates as composition and
character sources for derivative work. It does not delete, weaken, or relabel
the automated matte results or the independent reviewer report. Those records
correctly preserve that the current candidate WebPs are preview evidence rather
than guard-certified transparent masters.

## Downstream rules

- Use all three sets A, B, and C; do not collapse them to one winner.
- The active Hell lineage remains full-source generation followed by exact
  full-foreground RGB inversion. Never restore normal skin, repaint eyelids, or
  recolor the final tear after inversion.
- Preserve all nine superseded Hell references as recoverable evidence, but do
  not use them as downstream sources.
- Reuse the approved authority-katana pack, backgrounds, FX, dividers, icons,
  and character-free front-page plates where practical.
- Every derived asset must record its exact active source candidate and any
  deterministic extraction, crop, composite, scale, or format transform.
- Existing hard-guard findings remain visible in manifests and the showcase.
  Owner approval authorizes use; it does not claim that those checks passed.
- Workbench PNGs stay untracked and are backed up as-is. Production commits use
  WebP/SVG/JSON/HTML/docs/scripts, with a full PNG Lucy master only where the
  established exception applies.
