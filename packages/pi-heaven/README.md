# pi-heaven

> **STUB (WS5).** Name + intent only — no code claims.

The pi door to Skill Heaven, and the **vanguard**: pi's richer extension API
proves the full-fidelity interaction first — widget statusline, first-class
command registration, and the session-restart control (the "magic switch" rung
Claude Code can't verifiably reach). Findings port **down** to Claude Code's
more restrictive plugin surface (WS4).

Marketing weight stays on `claude-heaven` throughout: N9 settles the names and
D1 makes Claude Code the reference harness with pi second.

See `gaia-research/docs/plans/skill-heaven-continuation-plan.md` (WS5).
