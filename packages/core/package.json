{
  "name": "skill-heaven",
  "version": "0.1.0",
  "description": "Skill Heaven profile compiler + research driver (the skill-heaven bin: --print recipes, --record benchmark arms). The engine the per-harness doors are built on.",
  "type": "module",
  "bin": {
    "skill-heaven": "./bin/skill-heaven.mjs"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "launcher": "tsx src/cli.ts"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  }
}
