/**
 * Bumped whenever the index layout or the expansion generator changes, so a
 * bad generation run is revertible as data (SPEC §2.3 guardrail 2).
 */
export const INDEX_BUILDER_VERSION = "skill-index-builder/0.1.0";
