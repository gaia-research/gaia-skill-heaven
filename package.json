{
  "name": "skill-heaven",
  "version": "0.0.0",
  "description": "Strip your agent's context bloat — run clean. Skill Heaven profile compiler + research driver (per-harness doors: claude-heaven, pi-heaven, …).",
  "private": true,
  "type": "module",
  "bin": {
    "skill-heaven": "./bin/skill-heaven.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "launcher": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
