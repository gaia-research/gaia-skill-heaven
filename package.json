{
  "name": "gaia-skill-heaven-monorepo",
  "version": "0.0.0",
  "description": "gaia-skill-heaven monorepo — the Skill Zero launcher engine (packages/core, npm skill-zero) + per-harness doors (claude-zero, pi-zero, …), all under the Skill Heaven umbrella runtime brand. Doubles as the Claude Code plugin marketplace (.claude-plugin/marketplace.json).",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "launcher": "npm -w skill-zero run launcher --"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
