{
  "name": "skill-heaven-monorepo",
  "version": "0.0.0",
  "description": "Skill Heaven monorepo — shared profile-compiler engine (packages/core) + per-harness doors (claude-heaven, pi-heaven, …). Doubles as the plugin marketplace.",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "launcher": "npm -w skill-heaven run launcher --"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
